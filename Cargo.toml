[package]
name = "rust-2"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1"
image = "0.25"
serde = { version = "1", features = ["derive"] }
reqwest = { version = "0.12", default-features = false, features = [
    "blocking",
    "json",
    "rustls-tls",
] }
